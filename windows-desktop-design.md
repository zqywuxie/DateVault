# DateVault Windows 桌面应用设计

## 1. 目标

基于现有架构思路，将 DateVault 落地为一个轻量化 Windows 桌面应用。这里的“轻量化”包含四层含义：

1. 启动快
2. 占用低
3. 交互直接
4. 实现简单且维护成本低

## 2. 技术选型

### 2.1 首选方案

推荐采用：

```text
WPF + .NET 8 + CommunityToolkit.Mvvm
```

### 2.2 选择理由

1. 明确面向 Windows，不需要为跨平台支付额外复杂度。
2. 原生拖拽、剪贴板、文件系统、Explorer 集成成熟。
3. 启动速度和内存占用通常优于 Electron 方案。
4. UI 可以保持系统风格，降低视觉和交互学习成本。
5. 适合这种“文件操作工具型”应用，不需要 WebView 运行时。

### 2.3 不作为首选的方案

#### Electron

优点是快搭建，但包体、内存和启动成本不符合“轻量化优先”。

#### Tauri

如果后续明确需要跨平台，Tauri 是合理备选；但当前目标明确是 Windows 桌面，优先选择更直接的原生实现。

#### PySide6

开发快，但分发体积和桌面集成一致性一般，不作为第一选择。

## 3. 产品形态

DateVault 应该是一个单窗口工具型应用，不需要多窗口工作流。主窗口包含：

1. 顶部命令栏
2. 左侧目录树
3. 右侧操作与详情区
4. 底部状态栏

整体风格建议采用“Windows 工具型极简风格”：

1. 视觉低噪音
2. 高信息密度
3. 明确层次分隔
4. 轻强调色，不做花哨装饰

在 Windows 语境下，优先遵循系统视觉语言，使用 `Segoe UI Variable` 与标准控件风格即可。

## 4. 信息架构

### 4.1 主窗口结构

```text
┌──────────────────────────────────────────────────────────────┐
│ DateVault   根目录: E:\Archive   今日目录: 2026\03\26       │
│ [选择根目录] [归档到今天] [新建文件夹] [刷新] [设置]        │
├──────────────────────┬───────────────────────────────────────┤
│ 文件树               │ 快速归档                              │
│                      │ ┌───────────────────────────────────┐ │
│ 2026                 │ │ 将文件拖到这里，自动归档到今天目录 │ │
│   03                 │ └───────────────────────────────────┘ │
│     25               │                                       │
│     26               │ 当前选中                              │
│       合同           │ 名称: 2026\03\26                      │
│       发票           │ 路径: E:\Archive\2026\03\26           │
│                      │                                       │
│                      │ 最近操作                              │
│                      │ 10:31 已归档: invoice.pdf            │
│                      │ 10:32 已创建: 合同                    │
├──────────────────────┴───────────────────────────────────────┤
│ 就绪 | 目录节点 128 | 今日已归档 6 个文件                   │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 区域职责

#### 顶部命令栏

放置高频动作：

1. 选择根目录
2. 显示今日目录
3. 手动归档到今天
4. 新建文件夹
5. 刷新
6. 设置

#### 左侧文件树

用于浏览目录结构，支持：

1. 懒加载展开
2. 选中目录或文件
3. 右键菜单
4. 当前路径高亮

#### 右侧操作区

上半区是主操作区，下半区是详情和日志区。

这样可以让“拖拽归档”始终保持为第一视觉焦点。

#### 底部状态栏

显示：

1. 当前状态
2. 操作反馈
3. 已归档数量
4. 选中路径摘要

## 5. 核心页面与组件

### 5.1 MainWindow

负责布局容器和全局命令绑定。

### 5.2 TitleCommandBar

包含：

1. 应用标题
2. 根目录路径展示
3. 今日目录标签
4. 一级动作按钮

### 5.3 FileTreePane

包含：

1. TreeView
2. 节点图标
3. 右键菜单
4. 空状态提示

### 5.4 ArchiveDropPanel

包含：

1. 拖拽热区
2. 目标目录提示
3. 最近一次归档结果摘要

### 5.5 DetailLogPanel

可采用两个分区或 Tab：

1. 当前选中详情
2. 最近操作日志

### 5.6 SettingsDialog

只保留必要设置：

1. 根目录
2. 冲突策略
3. 默认创建位置
4. 是否开机记住窗口状态

## 6. 交互设计

## 6.1 首次启动

1. 没有配置文件时，进入空状态。
2. 主窗口仅显示“请选择根目录”。
3. 选择后立即创建或验证根目录，并加载第一层节点。

### 6.2 拖拽归档

1. 用户从 Explorer 拖入文件到右侧热区。
2. 热区进入高亮状态，显示“释放以归档到今天目录”。
3. 放下后立即执行归档。
4. 成功后：
   - 日志新增记录
   - 状态栏更新计数
   - 如果当前目录受影响，则自动刷新节点

### 6.3 创建文件夹

1. 点击“新建文件夹”。
2. 弹出轻量输入框，不需要复杂对话流。
3. 根据当前选中节点自动判断父目录。
4. 创建成功后，自动展开并选中新节点。

### 6.4 右键菜单

目录节点菜单：

1. 打开
2. 在资源管理器中打开
3. 新建子文件夹
4. 复制路径
5. 刷新当前节点

文件节点菜单：

1. 打开文件
2. 打开所在位置
3. 复制路径

### 6.5 日志反馈

日志不应成为弹窗打断来源。

推荐策略：

1. 成功操作写入底部或右侧日志
2. 失败操作显示非阻塞提示条
3. 严重错误才使用模态对话框

## 7. 视觉设计

### 7.1 风格方向

采用“档案柜 + Windows 工具面板”的视觉语言：

1. 以浅灰为主背景
2. 用低饱和蓝绿色做强调色
3. 使用细分隔线代替厚重卡片
4. 弱化装饰，强调扫描效率

### 7.2 颜色建议

```text
WindowBackground: #F5F6F8
Surface:          #FFFFFF
Border:           #D8DEE6
TextPrimary:      #1F2A37
TextSecondary:    #607080
Accent:           #2C7A7B
AccentSoft:       #E5F3F2
Success:          #2F855A
Warning:          #B7791F
Error:            #C53030
```

### 7.3 间距与尺寸

1. 主间距采用 8px 基线
2. 左树建议宽度 260px 到 300px
3. 顶部命令栏高度 52px
4. 底部状态栏高度 24px 到 28px
5. 拖拽区保持 96px 到 128px 高度，避免视觉臃肿

### 7.4 图标策略

优先使用系统风格的线性图标，不引入大体积图标库。只覆盖：

1. 文件夹
2. 文件
3. 打开
4. 刷新
5. 设置
6. 新建

## 8. 性能与轻量化指标

建议将以下指标写入实现验收标准：

1. 冷启动时间小于 1.5 秒
2. 空闲内存占用控制在 100 MB 以内
3. 展开普通目录节点的响应时间小于 150 ms
4. 归档单个常规文件时，主线程不明显阻塞
5. 启动时只读取根节点第一层，不递归预加载

## 9. 工程结构建议

```text
DateVault/
  src/
    DateVault.App/
      Views/
        MainWindow.xaml
        SettingsDialog.xaml
      ViewModels/
        MainWindowViewModel.cs
        FileTreeViewModel.cs
        ArchivePanelViewModel.cs
        LogPanelViewModel.cs
      Converters/
      Resources/
    DateVault.Application/
      UseCases/
        ArchiveFilesUseCase.cs
        CreateFolderUseCase.cs
        LoadTreeChildrenUseCase.cs
        ExecuteContextActionUseCase.cs
      DTOs/
      Abstractions/
    DateVault.Domain/
      Models/
      Services/
      Rules/
    DateVault.Infrastructure/
      FileSystem/
      Shell/
      Config/
      Logging/
```

## 10. 打包与发布建议

### 10.1 默认发布策略

Windows 内部使用场景下，优先采用：

```text
Framework-dependent + MSIX 或普通安装包
```

这样包体更小。

### 10.2 零依赖分发策略

如果目标机器环境不可控，可提供第二种构建：

```text
Self-contained single-file
```

这会增大包体，但能减少安装门槛。

## 11. MVP 实施范围

第一阶段只做以下内容：

1. 主窗口
2. 根目录配置
3. 文件树懒加载
4. 拖拽归档
5. 新建文件夹
6. 右键菜单
7. 日志面板

不进入第一阶段的内容：

1. 搜索
2. 标签
3. 多窗口
4. 文件预览
5. 自动监听全部目录变化

## 12. 最终结论

如果 DateVault 的目标是“Windows 上真正轻量、可靠、能长期维护的日期归档工具”，那么最合适的落地方式不是继续做泛跨平台设计，而是：

> 用原生 Windows 桌面技术实现一个单窗口、低状态、强文件系统集成的归档应用。

这会让产品更快可用，也更符合原始设计中“路径驱动、真实文件系统映射、最小复杂度”的核心原则。
