# DateVault 设计文档

## 1. 项目定位

DateVault 是一个以日期目录为核心规则的轻量文件归档工具。它面向日常资料整理场景，帮助用户把零散文件快速归档到 `YYYY/MM/DD` 目录，并以真实文件系统为唯一事实来源进行浏览和操作。

这个项目的重点不是做一个“文件内容管理平台”，而是做一个启动快、依赖少、学习成本低的 Windows 桌面归档器。

## 2. 设计目标

### 2.1 核心目标

1. 通过拖拽快速归档文件到当天日期目录。
2. 以文件树形式直观浏览归档根目录。
3. 支持新建文件夹、打开文件、打开所在目录、复制路径等高频操作。
4. 保持实现和运行都足够轻，不引入不必要的系统复杂度。

### 2.2 非目标

1. 不做数据库。
2. 不做文件内容索引。
3. 不做全文搜索引擎。
4. 不做云同步。
5. 不做复杂权限系统。
6. 不做媒体预览、缩略图缓存、标签系统等重型能力。

## 3. 核心设计原则

### 3.1 文件系统即唯一事实来源

文件树直接映射真实目录结构，应用不维护一套独立的数据副本。

### 3.2 日期目录是默认归档规则

所有归档行为默认落到：

```text
root/YYYY/MM/DD
```

如果用户主动切换目标目录，则以用户当前选择为准。

### 3.3 应用是流程控制器，不是存储系统

应用负责：

1. 路径规则生成
2. 文件移动与目录创建
3. Shell 集成
4. 界面展示与反馈

应用不负责：

1. 管理文件内容
2. 维护独立元数据仓库
3. 缓存整棵目录树

### 3.4 所有重量都后置

默认只实现高频、低成本、强确定性的能力。只有当真实场景证明必要时，才考虑增加搜索、标记、批处理规则等增强功能。

## 4. 用户场景

### 4.1 日常归档

用户从桌面、下载目录或聊天工具中拖入文件，应用自动归档到当天目录，并在右侧日志区显示结果。

### 4.2 资料整理

用户在左侧树中浏览年月日目录，在某个日期目录下创建子文件夹，如“合同”“发票”“截图”等。

### 4.3 快速定位

用户在树节点上右键，直接打开文件、打开所在位置，或者复制完整路径给其他软件使用。

## 5. 功能范围

### 5.1 必须具备

1. 根目录选择与保存
2. 今日目录显示
3. 拖拽文件归档
4. 新建文件夹
5. 文件树浏览
6. 懒加载树节点
7. 右键菜单
8. 日志反馈
9. 冲突处理

### 5.2 第一版可选

1. 最近归档记录
2. 手动刷新
3. 设置冲突策略
4. 最近使用目录

### 5.3 后续增强

1. 批量重命名
2. 固定常用目录
3. 简单筛选
4. 全局快捷键

## 6. 整体架构

采用四层结构，保持职责清晰：

```text
Presentation
  -> Application
    -> Domain
      -> Infrastructure
```

### 6.1 Presentation 层

负责用户交互、状态呈现和视图绑定，不直接操作文件系统。

### 6.2 Application 层

负责 UseCase 编排，决定什么时机调用哪些服务，并把结果转换成 UI 可消费的数据。

### 6.3 Domain 层

负责核心规则，包括日期路径规则、归档规则、冲突命名规则、树节点组织规则等。

### 6.4 Infrastructure 层

负责文件系统访问、系统 Shell 调用、剪贴板、配置读写、日志输出等系统交互。

## 7. 领域模型与服务划分

### 7.1 核心实体或模型

#### AppConfig

```text
rootPath
defaultTargetMode
conflictPolicy
windowState
```

#### TreeNode

```text
name
fullPath
nodeType
hasChildren
isExpanded
isLoaded
```

#### ArchiveItemResult

```text
sourcePath
targetPath
status
message
```

#### LogEntry

```text
timestamp
level
message
```

### 7.2 领域服务

#### DatePathService

职责：

1. 生成今天目录
2. 根据日期生成标准路径
3. 统一路径拼接逻辑

#### ArchiveService

职责：

1. 接收待归档文件列表
2. 解析目标目录
3. 创建缺失目录
4. 移动文件
5. 生成结果列表

#### FolderService

职责：

1. 计算创建位置
2. 校验文件夹名称
3. 创建文件夹
4. 处理文件夹重名冲突

#### TreeService

职责：

1. 读取目录节点
2. 将目录条目转换为树节点
3. 按规则排序
4. 支持懒加载

#### ConflictResolver

职责：

1. 对文件和文件夹冲突采用统一命名规则
2. 输出 `name(1)`、`name(2)` 形式的可用名称

#### PathResolver

职责：

1. 规范路径
2. 判断是文件还是目录
3. 获取父级目录

## 8. 基础设施接口

### 8.1 FileSystemGateway

提供：

1. 读取目录
2. 创建目录
3. 移动文件
4. 删除文件
5. 检查路径存在性

### 8.2 ShellGateway

提供：

1. 打开文件
2. 打开目录
3. 在资源管理器中定位文件

### 8.3 ConfigRepository

提供：

1. 读取本地 JSON 配置
2. 保存本地 JSON 配置

### 8.4 ClipboardGateway

提供：

1. 复制路径

### 8.5 Logger

提供：

1. UI 日志输出
2. 可选文件日志输出

## 9. 关键用例

### 9.1 归档文件

```text
用户拖拽文件
-> Presentation 接收拖拽事件
-> ArchiveFilesUseCase 执行
-> DatePathService 生成目标目录
-> ArchiveService 完成目录创建、冲突处理、文件移动
-> 返回 ArchiveItemResult[]
-> UI 刷新树与日志
```

### 9.2 创建文件夹

```text
用户点击新建文件夹
-> CreateFolderUseCase 计算目标目录
-> FolderService 校验名称并创建
-> UI 刷新对应节点
```

创建位置优先级：

```text
如果当前选中的是目录：使用当前目录
如果当前选中的是文件：使用其父目录
如果未选中任何节点：使用今日目录
```

### 9.3 加载树节点

```text
用户展开节点
-> LoadTreeChildrenUseCase
-> TreeService 读取当前目录
-> 返回排序后的 TreeNode 列表
-> UI 渲染
```

### 9.4 右键菜单

```text
用户右键节点
-> ContextActionUseCase
-> ShellGateway 或 ClipboardGateway
-> UI 显示执行结果
```

## 10. 交互规则

### 10.1 树节点排序

1. 文件夹优先
2. 同类型按名称升序

### 10.2 冲突处理

默认使用自动重命名：

```text
文件.txt -> 文件(1).txt -> 文件(2).txt
项目 -> 项目(1) -> 项目(2)
```

### 10.3 错误处理

需要明确反馈以下错误：

1. 根目录不存在
2. 无权限访问
3. 文件被占用
4. 路径非法
5. 路径过长
6. 重名处理失败

日志内容应包含失败原因，但不阻塞其他文件继续归档。

## 11. 轻量化约束

为了保持 DateVault 的轻量属性，设计上必须遵守以下约束：

1. 不引入数据库。
2. 不维护后台常驻扫描服务。
3. 不缓存整棵目录树。
4. 不生成缩略图。
5. 不接入浏览器内核级复杂前端框架作为首选实现。
6. 配置只用本地 JSON。
7. 启动时只加载必要界面和根节点，不预读所有目录。

## 12. 推荐项目结构

```text
DateVault/
  src/
    DateVault.App/
      Views/
      ViewModels/
      Commands/
    DateVault.Application/
      UseCases/
      DTOs/
      Abstractions/
    DateVault.Domain/
      Services/
      Models/
      Rules/
    DateVault.Infrastructure/
      FileSystem/
      Shell/
      Config/
      Logging/
  docs/
  assets/
```

## 13. 版本建议

### V1

聚焦“可用”：

1. 选择根目录
2. 拖拽归档
3. 浏览树
4. 新建文件夹
5. 右键菜单
6. 配置持久化

### V2

聚焦“顺手”：

1. 最近操作记录
2. 最近使用目录
3. 自定义目标日期
4. 可切换冲突策略

### V3

聚焦“增强”：

1. 快捷键扩展
2. 规则化归档
3. 轻量筛选与搜索

## 14. 结论

DateVault 最重要的设计决策只有一句话：

> 让真实文件系统保持简单，让桌面应用只承担最少但最高频的归档控制职责。

这决定了它应该是一个低依赖、低状态、低心智负担的工具，而不是一个复杂的资料管理平台。
